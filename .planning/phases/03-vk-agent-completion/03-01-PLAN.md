# PLAN 03-01: VK Photo Post Tool — Two-Step Upload Flow

---
wave: 1
depends_on: []
requirements: [VK-01]
files_modified:
  - services/agent-vk/internal/agent/handler.go
  - services/agent-vk/internal/vk/client.go
  - services/agent-vk/internal/agent/handler_test.go
  - services/orchestrator/cmd/main.go
autonomous: true
---

## Overview

Add `vk__post_photo` tool that downloads an image from a URL, uploads it to VK via the SDK's `UploadGroupWallPhoto` convenience method, then publishes a wall post with the photo attachment and optional caption.

## Tasks

<task id="03-01-01">
<title>Add PostPhoto method to VKClient interface and implementation</title>
<read_first>
- services/agent-vk/internal/agent/handler.go (VKClient interface definition, lines 19-23)
- services/agent-vk/internal/vk/client.go (full file — existing methods and vksdk usage)
</read_first>
<action>
1. In `services/agent-vk/internal/agent/handler.go`, add to the `VKClient` interface:
   ```go
   PostPhoto(groupID string, photoURL, caption string) (int64, error)
   ```

2. In `services/agent-vk/internal/vk/client.go`, implement `PostPhoto`:
   - Import `io`, `net/http`, `strconv`, `strings`, `fmt`
   - Download image: `http.Get(photoURL)` with a timeout (use `http.Client{Timeout: 30 * time.Second}`)
   - Validate response: check status 200, check Content-Type starts with `image/`
   - Defer `resp.Body.Close()`
   - Parse groupID: strip `-` prefix, convert to int with `strconv.Atoi`
   - Call `c.vk.UploadGroupWallPhoto(groupIDInt, resp.Body)` — returns `[]object.PhotosPhoto, error`
   - Check `len(photos) > 0`, otherwise return error "no photos returned from VK"
   - Format attachment: `fmt.Sprintf("photo%d_%d", photos[0].OwnerID, photos[0].ID)`
   - Call `c.vk.WallPost(vkapi.Params{"owner_id": "-" + strconv.Itoa(groupIDInt), "message": caption, "attachments": attachment})`
   - Return `int64(resp.PostID), nil`
</action>
<acceptance_criteria>
- `grep -n "PostPhoto" services/agent-vk/internal/agent/handler.go` shows the method in the VKClient interface
- `grep -n "func (c \*Client) PostPhoto" services/agent-vk/internal/vk/client.go` shows the implementation
- `grep -n "UploadGroupWallPhoto" services/agent-vk/internal/vk/client.go` confirms SDK usage
- `grep -n "attachments" services/agent-vk/internal/vk/client.go` confirms photo attachment in wall.post
</acceptance_criteria>
</task>

<task id="03-01-02">
<title>Add vk__post_photo handler case and input validation</title>
<read_first>
- services/agent-vk/internal/agent/handler.go (Handle switch, publishPost method pattern)
</read_first>
<action>
1. In `services/agent-vk/internal/agent/handler.go`, add a new case to the `Handle` switch:
   ```go
   case "vk__post_photo":
       return h.postPhoto(ctx, req)
   ```

2. Add the `postPhoto` method:
   ```go
   func (h *Handler) postPhoto(ctx context.Context, req a2a.ToolRequest) (*a2a.ToolResponse, error) {
       photoURL, _ := req.Args["photo_url"].(string)
       if photoURL == "" {
           return nil, a2a.NewNonRetryableError(fmt.Errorf("vk: photo_url is required"))
       }
       caption, _ := req.Args["caption"].(string)
       client, groupID, err := h.getClient(ctx, req)
       if err != nil {
           return nil, err
       }
       postID, err := client.PostPhoto(groupID, photoURL, caption)
       if err != nil {
           return nil, fmt.Errorf("vk: post photo: %w", classifyVKError(err))
       }
       return &a2a.ToolResponse{
           TaskID:  req.TaskID,
           Success: true,
           Result:  map[string]interface{}{"post_id": float64(postID)},
       }, nil
   }
   ```
</action>
<acceptance_criteria>
- `grep -n "vk__post_photo" services/agent-vk/internal/agent/handler.go` shows the switch case
- `grep -n "photo_url is required" services/agent-vk/internal/agent/handler.go` shows validation
- `grep -n "func (h \*Handler) postPhoto" services/agent-vk/internal/agent/handler.go` shows the method
</acceptance_criteria>
</task>

<task id="03-01-03">
<title>Add unit tests for vk__post_photo handler</title>
<read_first>
- services/agent-vk/internal/agent/handler_test.go (existing mock patterns, mockVKClient struct)
</read_first>
<action>
1. In `services/agent-vk/internal/agent/handler_test.go`, add `postPhotoFn` to `mockVKClient`:
   ```go
   postPhotoFn func(groupID string, photoURL, caption string) (int64, error)
   ```
   Add the method:
   ```go
   func (m *mockVKClient) PostPhoto(groupID string, photoURL, caption string) (int64, error) {
       return m.postPhotoFn(groupID, photoURL, caption)
   }
   ```

2. Add test `TestHandler_PostPhoto` — success case: verifies groupID, photoURL, caption are passed through, returns post_id.

3. Add test `TestHandler_PostPhoto_MissingURL` — empty photo_url returns NonRetryableError with "photo_url is required".

4. Add test `TestHandler_PostPhoto_VKError` — VK API error code 5 returns NonRetryableError.
</action>
<acceptance_criteria>
- `cd /Users/f1xgun/onevoice/services/agent-vk && go test -run TestHandler_PostPhoto -v ./internal/agent/` passes
- `grep -n "TestHandler_PostPhoto_MissingURL" services/agent-vk/internal/agent/handler_test.go` shows the validation test
</acceptance_criteria>
</task>

<task id="03-01-04">
<title>Register vk__post_photo tool in orchestrator</title>
<read_first>
- services/orchestrator/cmd/main.go (registerPlatformTools function, VK tools section lines 178-215)
</read_first>
<action>
1. In `services/orchestrator/cmd/main.go`, in the `registerPlatformTools` function, add to the VK agent's tools slice (after the existing `vk__publish_post` entry):

```go
{Type: "function", Function: llm.FunctionDefinition{
    Name:        "vk__post_photo",
    Description: "Публикует пост с фото и текстовой подписью на стену сообщества ВКонтакте. Используй эту функцию вместо publish_post когда нужно опубликовать пост с изображением.",
    Parameters: map[string]interface{}{
        "type": "object",
        "properties": map[string]interface{}{
            "photo_url": map[string]interface{}{"type": "string", "description": "Публичный URL изображения для загрузки"},
            "caption":   map[string]interface{}{"type": "string", "description": "Текстовая подпись к фото"},
            "group_id":  map[string]interface{}{"type": "string", "description": "ID сообщества ВКонтакте"},
        },
        "required": []string{"photo_url"},
    },
}},
```

2. Update the existing `vk__publish_post` description to clarify it is text-only:
   Change to: `"Публикует текстовый пост (без фото) на стену сообщества ВКонтакте. Если нужно опубликовать пост с фото — используй vk__post_photo вместо этого."`
</action>
<acceptance_criteria>
- `grep -n "vk__post_photo" services/orchestrator/cmd/main.go` shows the tool registration
- `grep -n "без фото" services/orchestrator/cmd/main.go` shows the updated publish_post description
- `cd /Users/f1xgun/onevoice/services/orchestrator && go build ./cmd/` compiles without errors
</acceptance_criteria>
</task>

## Verification

### must_haves
- `VKClient` interface includes `PostPhoto(groupID string, photoURL, caption string) (int64, error)`
- `client.go` implementation downloads image, calls `UploadGroupWallPhoto`, formats attachment, calls `WallPost`
- Handler validates `photo_url` is non-empty, returns `NonRetryableError` on missing
- Handler switch includes `vk__post_photo` case
- Orchestrator registers `vk__post_photo` tool with `photo_url` required param
- `vk__publish_post` description clarifies text-only to avoid LLM confusion with `vk__post_photo`
- All existing tests still pass: `cd services/agent-vk && go test ./...`
- New photo tests pass: `cd services/agent-vk && go test -run TestHandler_PostPhoto ./internal/agent/`
- Orchestrator compiles: `cd services/orchestrator && go build ./cmd/`
