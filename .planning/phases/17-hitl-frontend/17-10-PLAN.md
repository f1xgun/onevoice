---
phase: 17-hitl-frontend
plan: 10
type: execute
wave: 2
depends_on: [07, 08, 09]
gap_closure: true
files_modified:
  - .planning/phases/17-hitl-frontend/17-VERIFICATION.md
autonomous: false
requirements: [UI-08, UI-09]
must_haves:
  truths:
    - "All ten verification matrix items from Plan 17-06 pass against the gap-closed stack"
    - "GAP-01, GAP-02, GAP-03 from 17-VERIFICATION.md are confirmed closed"
    - "Items 8 (expired banner), 9 (keyboard nav), 10 (screen reader) — previously deferred — are now exercised"
    - "17-VERIFICATION.md is updated to status: verified with the new score and closing summary"
  artifacts:
    - path: .planning/phases/17-hitl-frontend/17-VERIFICATION.md
      provides: "Updated verification report with new matrix + status: verified + closing notes"
  key_links:
    - from: 17-07 (backend wiring)
      to: 17-10 verification matrix item 7 (reload mid-approval)
      via: "Plan 17-07's repository guard + handler threading make hydration reachable"
      pattern: "PASS"
    - from: 17-08 (read-only args + edit hint)
      to: 17-10 verification matrix items 2 + 3
      via: "Args visible without entering Edit; affordance hint is discoverable"
      pattern: "PASS"
    - from: 17-09 (UI polish)
      to: 17-10 verification matrix items 4 + 6
      via: "Submit hint hides on enabled; 403 toast is scope-accurate"
      pattern: "PASS"
---

<objective>
Re-execute the 17-06 human-verify checklist against the gap-closed stack. Confirm GAP-01 / GAP-02 / GAP-03 are closed; exercise items 8–10 that were deferred in the original 17-06 pass; update `17-VERIFICATION.md` from `status: gaps_found` to `status: verified` (or open new gaps if any remain).

Purpose: Definitive sign-off that Phase 17 ships. Automated tests in Plans 17-07/08/09 give regression coverage, but only a live human-verified pass through the real Mongo + browser confirms the user-visible experience is whole.

Output: Updated `17-VERIFICATION.md` (status, score, matrix, closing notes), or — if any item still fails — a follow-up gap-closure recommendation.
</objective>

<execution_context>
@/Users/f1xgun/onevoice/.worktrees/milestone-1.3/.claude/get-shit-done/workflows/execute-plan.md
@/Users/f1xgun/onevoice/.worktrees/milestone-1.3/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/17-hitl-frontend/17-VERIFICATION.md
@.planning/phases/17-hitl-frontend/17-CONTEXT.md
@.planning/phases/17-hitl-frontend/17-UI-SPEC.md
@.planning/phases/17-hitl-frontend/17-06-PLAN.md
@.planning/phases/17-hitl-frontend/17-06-SUMMARY.md
@.planning/phases/17-hitl-frontend/17-07-PLAN.md
@.planning/phases/17-hitl-frontend/17-08-PLAN.md
@.planning/phases/17-hitl-frontend/17-09-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Re-verify Phase 17 against the gap-closed stack and update 17-VERIFICATION.md</name>
  <files>.planning/phases/17-hitl-frontend/17-VERIFICATION.md</files>
  <action>
    Stand up the local stack and execute the 10-row verification matrix from 17-06-PLAN.md against the gap-closed code (Plans 17-07, 17-08, 17-09 must already be merged). Record PASS/FAIL per row and update `.planning/phases/17-hitl-frontend/17-VERIFICATION.md` per the detailed instructions in `<how-to-verify>` below. The action is observational + documentary — no source code changes are made by this plan.
  </action>
  <verify>
    <automated>node /Users/f1xgun/onevoice/.claude/get-shit-done/bin/gsd-tools.cjs frontmatter validate .planning/phases/17-hitl-frontend/17-VERIFICATION.md --schema verification</automated>
  </verify>
  <done>17-VERIFICATION.md frontmatter is updated to `status: verified` (or `gaps_found` with new GAP-XX entries if any item failed); the matrix table reflects the new run; the closing summary documents which gap-closure plans executed.</done>
  <what-built>
    Plans 17-07, 17-08, 17-09 closed three high/critical gaps + two side findings:
    - 17-07: Backend identity-field threading (chat_proxy + orchestrator handler + repository guard) so PendingToolCallBatch persists with non-empty conversation_id / business_id / user_id / message_id.
    - 17-08: Read-only Аргументы block always visible when an entry is expanded; affordance hint chip in Edit mode (`Дважды нажмите на значение, чтобы изменить`); useChat hydration regression test.
    - 17-09: Submit hint copy hides on enabled state; 403 toast routes to dedicated `Отказано: операция вне вашей бизнес-области` copy.

    This plan re-executes the full 17-06 verification matrix end-to-end against a running stack and writes the updated 17-VERIFICATION.md.
  </what-built>
  <how-to-verify>
    **Setup:** Stand up the local stack (`make dev` or `docker compose up -d`). Confirm `api`, `orchestrator`, `frontend`, `nginx`, `postgres`, `mongodb`, `nats`, `redis` are all healthy. Authenticate as `test@test.test` in the browser.

    Re-execute the 10-row matrix from 17-06-PLAN.md. For each row, record `PASS` / `FAIL` + a one-line note. The matrix:

    1. **Card renders above composer on pause** — Send a message that triggers a `manual`-floor tool (`telegram__send_channel_post` with a Telegram channel integration active). Expected: card with `aria-labelledby="approval-card-title"` appears above the composer; composer is disabled; badge `Ожидает подтверждения (1)`; subtitle `Проверьте аргументы перед выполнением`.

    2. **Accordion + toggle flow (GAP-01 closure target)** — Click the chevron on the entry. Expected (NEW behavior after 17-08): args are visible immediately as a read-only JSON view, alongside the three toggle buttons (Одобрить / Изменить / Отклонить). The `Аргументы` heading is present without selecting Edit. The `Можно изменять: text` hint chip is also visible.

    3. **JSON editor field whitelist (GAP-02 closure target)** — Click `Изменить`. Expected (NEW behavior after 17-08): the affordance hint chip `Дважды нажмите на значение, чтобы изменить` appears above the JSON editor. Double-click the editable value (e.g. the `text` field's value). Expected: an inline input/contenteditable opens; type a new value; press Enter; the edit is accepted.

    4. **Submit gating (item 4 closure target)** — Before any decision, Submit is `aria-disabled="true"` and tooltip `Выберите действие для каждой задачи` shows on hover. Pick a decision. Expected (NEW behavior after 17-09): Submit transitions to enabled (`aria-disabled="false"` or absent); the helper hint disappears from the DOM (verify with browser devtools: `document.querySelector('[id="approval-card-submit-helper"]')` returns `null`).

    5. **Atomic Submit + resume SSE (GAP-03 + item 5 closure target)** — Click Подтвердить. Expected (NEW behavior after 17-07): `POST /pending-tool-calls/{batch_id}/resolve` returns **200** (NOT 403); the resume SSE stream opens; the assistant message completes inline (same message bubble); the card unmounts; the composer re-enables. Verify in Mongo: `db.pending_tool_calls.findOne({_id: "<batch>"})` shows `status: "resolved"` (or "dispatched" → "resolved" depending on the resolve flow — both are acceptable terminal states).

    6. **Error handling (item 6 closure target)** — Force a 403 (e.g. by manually changing the JWT subject in localStorage to a different user, or by manipulating the request body's session token). Click Submit. Expected (NEW behavior after 17-09): toast text is `Отказано: операция вне вашей бизнес-области` (NOT the generic connection error). Restore the JWT.

    7. **Reload mid-approval (GAP-03 closure target)** — Trigger a fresh paused turn (don't resolve). Refresh the browser tab (Cmd+R). Expected (NEW behavior after 17-07): the approval card reappears above the composer; composer is disabled; the same args are visible. Verify in Mongo: `db.pending_tool_calls.findOne({status:"pending"})` has all four IDs non-empty.

    8. **Expired batch banner (previously deferred)** — Manually expire a pending batch via Mongo: `db.pending_tool_calls.updateOne({_id: "<batch>"}, {$set: {expires_at: new Date(Date.now() - 86400000)}})`. Reload the browser tab. Expected: `ExpiredApprovalBanner` renders above where the card would have been; the card itself does NOT render (per Phase-16 D-19); banner copy is `Эта операция истекла — отправьте новое сообщение, чтобы продолжить.`; banner has a dismiss `X` button with `aria-label="Закрыть сообщение"`.

    9. **Keyboard-only navigation (previously deferred)** — Trigger a paused turn. Without using the mouse, navigate from the page entry point: Tab to the first accordion entry's chevron trigger, Tab to Approve, Tab to Edit, Tab to Reject, then to Submit (when allDecided). Press `Enter` / `Space` on each — expected: trigger toggles expand state; toggle buttons activate; Enter on Submit fires the resolve. Press `Escape` on a focused accordion entry — expected: the entry collapses (Radix default).

    10. **Screen-reader spot check (previously deferred — best-effort)** — On macOS, enable VoiceOver (Cmd+F5). Trigger a paused turn. Walk through the card with VO+Right Arrow. Expected announcements:
        - `region: Ожидает подтверждения, 1 задач`
        - For each accordion: `{toolName} — развернуть` (collapsed) / `свернуть` (expanded)
        - For each toggle: `Одобрить {toolName}, кнопка` / `Изменить {toolName}, кнопка` / `Отклонить {toolName}, кнопка`
        - For the Submit button: `Подтвердить, кнопка` (when allDecided) — note: when `aria-disabled` is removed (per 17-09 Task 1), the button must NOT announce as "dimmed" or "unavailable".
        - Verify the helper hint is NOT announced when allDecided (`aria-describedby` is undefined per the 17-09 refinement).

    **After running the matrix:**

    Update `.planning/phases/17-hitl-frontend/17-VERIFICATION.md`:

    - Change the frontmatter:
      - `verified:` → today's ISO datetime
      - `status:` → `verified` (assuming all 10 PASS) or `gaps_found` (if any FAIL — surface them as new GAP-XX entries)
      - `score:` → `10/10` if all PASS; otherwise `N/10` matching the new pass count
      - Add a new line: `gap_closure_plans_executed: [17-07, 17-08, 17-09]`

    - Replace the `## Verification Matrix` table with the new 10-row results.

    - Add a new section `## Gap Closure Re-Verification (2026-04-XX)` documenting:
      - The three plans executed (17-07, 17-08, 17-09)
      - The matrix outcome
      - Confirmation that the original GAP-01, GAP-02, GAP-03 are closed (or document any residual scope)
      - Any new gaps surfaced during this re-verification pass

    - Keep the original GAP-01 / GAP-02 / GAP-03 sections in the file as historical record, but add a `**Status:** CLOSED by Plan 17-XX (YYYY-MM-DD)` header line at the top of each.

    - Update the `## Recommended Next Step` at the bottom to either:
      - All PASS: `Phase 17 is complete. Mark 17-06-PLAN.md and the phase entry in ROADMAP.md as done. Ready for milestone v1.3 progression.`
      - Some FAIL: Recommend `/gsd-plan-phase 17 --gaps` against the new gaps.

    Reply `approved` if all 10 PASS and 17-VERIFICATION.md is updated. Reply with details if any FAIL.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live browser → live API → live orchestrator → live Mongo | This plan exercises the full vertical at runtime; trust is assumed correctly enforced by upstream plans. |
| Operator-driven mongosh manual edit (item 8) | The operator manipulates the database directly to simulate expiration. This is intentional, scoped to the verification environment, and reverted post-test. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-10-01 | Tampering | Operator-edited Mongo state for item 8 (expired banner) | accept | Verification environment only; the manipulation is reverted at end of session by the operator. No production data affected. |
| T-17-10-02 | Spoofing | Item 6 forces a 403 by manipulating JWT subject | accept | The operator is verifying their own UI behavior. The JWT manipulation is local-only (browser localStorage); no auth backend is bypassed. |
| T-17-10-03 | Information Disclosure | Verification report records mongosh output | mitigate | The 17-VERIFICATION.md update should redact any business-specific identifiers (UUIDs may be partially redacted). The "non-empty" assertion is sufficient — full IDs are not needed for the historical record. |

</threat_model>

<verification>
- All ten matrix items from 17-06-PLAN.md PASS in a single end-to-end session.
- 17-VERIFICATION.md frontmatter updated to `status: verified` with `score: 10/10`.
- New "Gap Closure Re-Verification" section documents the matrix, the closed gaps, and any residual scope.
- ROADMAP.md Phase 17 entry updated: `5/6 plans executed` → `10/10 plans executed` (or whatever the final count is after 17-07 through 17-10 land).
</verification>

<success_criteria>
- A live human-verified pass against the gap-closed stack confirms Phase 17 ships.
- All three originally-discovered gaps (GAP-01, GAP-02, GAP-03) and the two side findings (items 4, 6) are closed.
- Items 8, 9, 10 (previously deferred) are exercised; any issues are surfaced as new gaps OR confirmed PASS.
- 17-VERIFICATION.md tells the complete story: original gaps → closure plans → re-verification result.
</success_criteria>

<output>
After completion, create `.planning/phases/17-hitl-frontend/17-10-SUMMARY.md` documenting:
- The matrix outcome (10 rows, PASS/FAIL each)
- Confirmation that 17-VERIFICATION.md is updated to verified
- Any new gaps surfaced (should be zero if all plans land cleanly)
- Recommendation for next step (mark phase complete, or run another gap-closure round)

Also update `.planning/STATE.md` and `.planning/ROADMAP.md`:
- STATE.md: Move Phase 17 from `In Progress` to `COMPLETE` with the verified date.
- ROADMAP.md: Phase 17 entry — change `5/6` to the final plan count (likely `9/10` or `10/10` after 17-06 → 17-10) and the status to `Complete`.
</output>
