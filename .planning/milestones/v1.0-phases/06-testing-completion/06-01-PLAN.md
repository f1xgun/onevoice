---
plan: "06-01"
title: "Auth flow test suite"
phase: 6
wave: 1
depends_on: []
requirements: ["TST-03"]
files_modified:
  - "services/api/internal/handler/auth_test.go"
  - "services/api/internal/middleware/auth_test.go"
  - "services/api/internal/middleware/ratelimit_test.go"
autonomous: true
---

## Tasks

<task id="6-01-01">
<title>Add ChangePassword handler tests and secure cookie tests to auth_test.go</title>
<read_first>
- services/api/internal/handler/auth.go (ChangePassword handler, cookie methods)
- services/api/internal/handler/auth_test.go (existing tests, MockUserService)
- services/api/internal/middleware/auth.go (middleware.UserIDKey)
</read_first>
<action>
Append to `services/api/internal/handler/auth_test.go`:

1. **TestChangePassword** — table-driven tests covering:
   - successful password change (userID in context, valid body) → 200 `{"status":"ok"}`
   - missing userID in context → 401 unauthorized
   - invalid JSON body → 400
   - validation failure (newPassword too short, empty currentPassword) → 400
   - wrong current password (service returns `domain.ErrInvalidCredentials`) → 401
   - user not found (service returns `domain.ErrUserNotFound`) → 404
   - internal server error → 500 (no internal detail leak)

2. **TestNewAuthHandler_NilService** — verify `NewAuthHandler(nil, false)` returns error.

3. **TestSecureCookies** — create handler with `secureCookies=true`:
   - Login success → cookie name is `__Host-refresh_token`, `Secure=true`
   - Refresh with `__Host-refresh_token` cookie → succeeds
   - Refresh with plain `refresh_token` cookie → also succeeds (upgrade path in `readRefreshTokenCookie`)

4. **TestRegister_AutoLoginFailure** — Register succeeds but auto-Login fails → 500

Use existing `MockUserService` and `middleware.UserIDKey` context injection pattern from TestMe.
</action>
<acceptance_criteria>
- `TestChangePassword` has at least 7 sub-tests covering all error branches
- `TestNewAuthHandler_NilService` verifies nil service is rejected
- `TestSecureCookies` verifies `__Host-refresh_token` name and `Secure=true` flag
- `TestSecureCookies` verifies upgrade-path reading of both cookie names
- `TestRegister_AutoLoginFailure` covers the auto-login error branch
- `cd services/api && GOWORK=off go test -race -run "TestChangePassword|TestNewAuthHandler|TestSecureCookies|TestRegister_AutoLoginFailure" ./internal/handler/...` passes
</acceptance_criteria>
</task>

<task id="6-01-02">
<title>Add JWT signing method and issuer/audience validation tests to auth middleware</title>
<read_first>
- services/api/internal/middleware/auth.go (Auth middleware, jwt.ParseWithClaims options)
- services/api/internal/middleware/auth_test.go (existing tests, createTestToken helper)
- services/api/internal/auth/claims.go (TokenIssuer, TokenAudience constants)
</read_first>
<action>
Append to `services/api/internal/middleware/auth_test.go`:

1. **TestAuth_WrongSigningMethod** — create a token signed with `jwt.SigningMethodHS384` (not HS256) using the correct secret. Verify middleware returns 401 `token_invalid`. This validates `jwt.WithValidMethods([]string{"HS256"})`.

2. **TestAuth_WrongIssuer** — create a token with `Issuer: "wrong-issuer"` but correct secret and HS256. Verify middleware returns 401 `token_invalid`. This validates `jwt.WithIssuer(auth.TokenIssuer)`.

3. **TestAuth_WrongAudience** — create a token with `Audience: jwt.ClaimStrings{"wrong-audience"}` but correct secret/issuer/HS256. Verify middleware returns 401 `token_invalid`. This validates `jwt.WithAudience(auth.TokenAudience)`.

4. **TestAuth_NoneAlgorithm** — create an unsigned JWT (alg: none) to verify the middleware rejects it. Build the token string manually: base64url-encode `{"alg":"none","typ":"JWT"}` header + valid claims + empty signature. Verify 401.

For tests 1-3, use `jwt.NewWithClaims` directly with `auth.AccessTokenClaims` (not the helper) to set specific issuer/audience/signing method.
</action>
<acceptance_criteria>
- `TestAuth_WrongSigningMethod` proves HS384 token is rejected even with correct secret
- `TestAuth_WrongIssuer` proves wrong issuer is rejected
- `TestAuth_WrongAudience` proves wrong audience is rejected
- `TestAuth_NoneAlgorithm` proves unsigned JWT is rejected
- `cd services/api && GOWORK=off go test -race -run "TestAuth_Wrong|TestAuth_None" ./internal/middleware/...` passes
</acceptance_criteria>
</task>

<task id="6-01-03">
<title>Add RateLimitByUser and Redis-failure tests to ratelimit_test.go</title>
<read_first>
- services/api/internal/middleware/ratelimit.go (RateLimitByUser, RateLimit)
- services/api/internal/middleware/ratelimit_test.go (existing tests, setupTestRedis helper)
- services/api/internal/middleware/auth.go (UserIDKey)
</read_first>
<action>
Append to `services/api/internal/middleware/ratelimit_test.go`:

1. **TestRateLimitByUser_WithinLimit** — set `UserIDKey` in context, make requests within limit → 200, verify `X-RateLimit-Remaining` headers decrement.

2. **TestRateLimitByUser_ExceedsLimit** — set `UserIDKey` in context, exceed limit → 429 with `rate limit exceeded` and `Retry-After` header.

3. **TestRateLimitByUser_FallbackToIP** — no `UserIDKey` in context, verify rate limiting still works using IP-based key.

4. **TestRateLimitByUser_DifferentUsers** — two different user UUIDs → independent rate limit counters.

5. **TestRateLimit_RedisDown** — close the miniredis server before making requests. Verify the middleware allows the request through (fail-open behavior). Test this by calling `mr.Close()` after `setupTestRedis`, then making a request → expect 200.

6. **TestRateLimit_RetryAfterHeader** — exceed limit, verify `Retry-After` header is a positive integer.

Use `setupTestRedis(t)` helper and `context.WithValue(r.Context(), UserIDKey, userUUID)` for context injection.

Import `"github.com/google/uuid"` (add to existing imports).
</action>
<acceptance_criteria>
- `TestRateLimitByUser_WithinLimit` passes with correct header values
- `TestRateLimitByUser_ExceedsLimit` returns 429 with proper error response
- `TestRateLimitByUser_FallbackToIP` shows IP-based limiting when no user context
- `TestRateLimitByUser_DifferentUsers` proves user isolation
- `TestRateLimit_RedisDown` proves fail-open behavior
- `TestRateLimit_RetryAfterHeader` verifies header presence and positive value
- `cd services/api && GOWORK=off go test -race -run "TestRateLimitByUser|TestRateLimit_RedisDown|TestRateLimit_RetryAfterHeader" ./internal/middleware/...` passes
</acceptance_criteria>
</task>

## Verification

### must_haves
- [ ] `TestChangePassword` covers all 7 error branches in ChangePassword handler
- [ ] `TestSecureCookies` verifies `__Host-refresh_token` cookie name and `Secure=true`
- [ ] `TestAuth_WrongSigningMethod` rejects HS384 token
- [ ] `TestAuth_WrongIssuer` rejects wrong issuer
- [ ] `TestAuth_WrongAudience` rejects wrong audience
- [ ] `TestAuth_NoneAlgorithm` rejects unsigned JWT
- [ ] `TestRateLimitByUser` has tests for within-limit, exceeds-limit, fallback-to-IP, user isolation
- [ ] `TestRateLimit_RedisDown` proves fail-open behavior
- [ ] `cd services/api && GOWORK=off go test -race ./internal/handler/...` passes
- [ ] `cd services/api && GOWORK=off go test -race ./internal/middleware/...` passes

## PLANNING COMPLETE
