---
phase: 09-frontend-telemetry
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - services/frontend/app/(app)/layout.tsx
  - services/frontend/hooks/useChat.ts
  - services/frontend/lib/telemetry.ts
autonomous: true
requirements: [FLG-01]

must_haves:
  truths:
    - "Page navigations are tracked as telemetry events"
    - "Chat message sends are tracked as telemetry events"
    - "Key button clicks (create conversation, connect integration) are tracked"
    - "Telemetry calls do not degrade UI responsiveness"
  artifacts:
    - path: "services/frontend/app/(app)/layout.tsx"
      provides: "Navigation tracking via pathname changes"
      contains: "trackEvent"
    - path: "services/frontend/hooks/useChat.ts"
      provides: "Chat send tracking"
      contains: "trackEvent"
  key_links:
    - from: "services/frontend/app/(app)/layout.tsx"
      to: "services/frontend/lib/telemetry.ts"
      via: "trackEvent import and usePathname effect"
      pattern: "trackEvent.*page_view"
    - from: "services/frontend/hooks/useChat.ts"
      to: "services/frontend/lib/telemetry.ts"
      via: "trackEvent on message send"
      pattern: "trackEvent.*chat_send"
---

<objective>
Wire telemetry tracking into the frontend app — page navigation, chat sends, and key button clicks.

Purpose: With the telemetry module and backend endpoint in place (Plan 01), this plan instruments the actual user actions so meaningful events flow through to Loki.

Output: Navigation events tracked on every route change, chat sends tracked, key interaction points instrumented.
</objective>

<execution_context>
@/Users/f1xgun/onevoice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/f1xgun/onevoice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-frontend-telemetry/09-01-SUMMARY.md

@services/frontend/app/(app)/layout.tsx
@services/frontend/hooks/useChat.ts
@services/frontend/lib/telemetry.ts

<interfaces>
<!-- From Plan 01 (telemetry module) -->
From services/frontend/lib/telemetry.ts:
```typescript
export function trackEvent(
  eventType: string,
  action: string,
  opts?: {
    page?: string;
    correlationId?: string;
    metadata?: Record<string, string>;
  }
): void;

export function flushTelemetry(): Promise<void>;
```

Event types to use:
- "page_view" — navigation events
- "chat_send" — user sends a chat message
- "button_click" — key UI interactions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add page navigation and chat send telemetry</name>
  <files>
    services/frontend/app/(app)/layout.tsx,
    services/frontend/hooks/useChat.ts
  </files>
  <read_first>
    services/frontend/app/(app)/layout.tsx,
    services/frontend/hooks/useChat.ts,
    services/frontend/lib/telemetry.ts
  </read_first>
  <action>
    1. Update `services/frontend/app/(app)/layout.tsx`:
       - Import `usePathname` from `next/navigation` (already using `useRouter`)
       - Import `{ trackEvent }` from `@/lib/telemetry`
       - Add `const pathname = usePathname();`
       - Add a `useEffect` that fires on `pathname` change:
         ```typescript
         useEffect(() => {
           trackEvent('page_view', pathname, { page: pathname });
         }, [pathname]);
         ```
       - This effect should be AFTER the auth-ready check (inside the component body but separate from the auth effect)
       - The trackEvent call is synchronous (just pushes to buffer), so zero UI impact

    2. Update `services/frontend/hooks/useChat.ts`:
       - Import `{ trackEvent }` from `@/lib/telemetry`
       - In the `sendMessage` function (or wherever the user's message is sent to the SSE endpoint), add right before or after the fetch call:
         ```typescript
         trackEvent('chat_send', 'send_message', {
           metadata: { conversationId: conversationId ?? '' },
         });
         ```
       - This should NOT be inside the SSE event loop — only on the initial send action
  </action>
  <verify>
    <automated>cd /Users/f1xgun/onevoice/services/frontend && pnpm lint && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "trackEvent" services/frontend/app/\(app\)/layout.tsx
    - grep -q "page_view" services/frontend/app/\(app\)/layout.tsx
    - grep -q "usePathname" services/frontend/app/\(app\)/layout.tsx
    - grep -q "trackEvent" services/frontend/hooks/useChat.ts
    - grep -q "chat_send" services/frontend/hooks/useChat.ts
  </acceptance_criteria>
  <done>
    Page navigations fire page_view telemetry events on every route change. Chat sends fire chat_send events. TypeScript compiles, lint passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add key button click telemetry to integration and conversation pages</name>
  <files>
    services/frontend/lib/telemetry.ts
  </files>
  <read_first>
    services/frontend/lib/telemetry.ts,
    services/frontend/app/(app)/integrations/page.tsx,
    services/frontend/components/integrations/VKCommunityModal.tsx
  </read_first>
  <action>
    1. Add a convenience wrapper to `services/frontend/lib/telemetry.ts`:
       ```typescript
       export function trackClick(action: string, metadata?: Record<string, string>): void {
         trackEvent('button_click', action, { metadata });
       }
       ```

    2. Instrument key interaction points. For each page/component below, add `trackClick` calls on the primary action buttons. The exact components to instrument depend on what Plan 01 summary reveals about the codebase, but target these actions:
       - Integration page: "connect_integration" when user initiates platform connection
       - Conversation list: "create_conversation" when user creates new conversation
       - Any "delete" confirmation actions: "delete_integration", "delete_conversation"

    3. The trackClick calls are fire-and-forget (push to buffer), so they add zero latency to click handlers. Add them as the first line in onClick handlers or in the mutation's onSuccess callback, whichever is more appropriate.

    NOTE: Keep instrumentation minimal — only 3-5 key buttons. Do NOT instrument every single button. The goal is actionable telemetry, not exhaustive coverage. If the specific component files are complex, prefer adding trackClick at the page level (in mutation onSuccess callbacks) rather than deep inside component trees.
  </action>
  <verify>
    <automated>cd /Users/f1xgun/onevoice/services/frontend && pnpm lint && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "trackClick" services/frontend/lib/telemetry.ts
    - grep -rq "trackClick\|trackEvent.*button_click" services/frontend/app/\(app\)/ || grep -rq "trackClick\|trackEvent.*button_click" services/frontend/components/
  </acceptance_criteria>
  <done>
    Key button clicks are instrumented with telemetry (3-5 primary actions). trackClick convenience wrapper exported. TypeScript compiles, lint passes. Telemetry does not degrade UI responsiveness (all calls are buffer pushes).
  </done>
</task>

</tasks>

<verification>
1. Frontend compiles: `cd services/frontend && pnpm exec tsc --noEmit`
2. Frontend lints: `cd services/frontend && pnpm lint`
3. Navigation tracking wired: `grep -q "page_view" services/frontend/app/\(app\)/layout.tsx`
4. Chat tracking wired: `grep -q "chat_send" services/frontend/hooks/useChat.ts`
5. Button tracking exists: `grep -rq "trackClick" services/frontend/`
</verification>

<success_criteria>
- Page navigation fires page_view events on every route change via usePathname + useEffect
- Chat sends fire chat_send events
- 3-5 key buttons are instrumented with trackClick
- All telemetry is non-blocking (buffer push, no awaits in UI thread)
- TypeScript compiles clean, ESLint passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-frontend-telemetry/09-02-SUMMARY.md`
</output>
