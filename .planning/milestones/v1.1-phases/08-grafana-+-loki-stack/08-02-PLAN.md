---
phase: 08-grafana-+-loki-stack
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - observability/grafana/dashboards/request-trace.json
  - observability/grafana/dashboards/metrics-overview.json
autonomous: true
requirements: [LOG-02, LOG-03]

must_haves:
  truths:
    - "Entering a correlation_id in the Request Trace dashboard shows logs from API, orchestrator, and agents in chronological order"
    - "Metrics Overview dashboard shows HTTP request latency percentiles (p50/p95/p99)"
    - "Metrics Overview dashboard shows error rate by service"
    - "Metrics Overview dashboard shows tool dispatch duration histogram"
    - "Dashboards survive container restarts because they are provisioned as JSON files"
  artifacts:
    - path: "observability/grafana/dashboards/request-trace.json"
      provides: "Loki-based correlation_id log search dashboard"
      contains: "correlation_id"
    - path: "observability/grafana/dashboards/metrics-overview.json"
      provides: "Prometheus metrics dashboard"
      contains: "http_request_duration_seconds"
  key_links:
    - from: "observability/grafana/dashboards/request-trace.json"
      to: "Loki datasource"
      via: "datasource uid reference"
      pattern: "loki"
    - from: "observability/grafana/dashboards/metrics-overview.json"
      to: "Prometheus datasource"
      via: "datasource uid reference"
      pattern: "prometheus"
    - from: "observability/grafana/provisioning/dashboards/dashboards.yml"
      to: "observability/grafana/dashboards/"
      via: "file provisioning path"
      pattern: "/var/lib/grafana/dashboards"
---

<objective>
Create Grafana dashboard JSON files for log search and metrics visualization.

Purpose: Give developers a single place to (1) trace requests across services by correlation_id and (2) monitor HTTP latency, error rates, and tool dispatch performance.

Output: Two provisioned Grafana dashboard JSON files.
</objective>

<execution_context>
@/Users/f1xgun/onevoice/.claude/get-shit-done/workflows/execute-plan.md
@/Users/f1xgun/onevoice/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-grafana-+-loki-stack/08-01-SUMMARY.md

<interfaces>
<!-- Prometheus metrics available for dashboard queries -->

http_requests_total{method, path, status}          — counter
http_request_duration_seconds{method, path, status} — histogram (DefBuckets)
tool_dispatch_total{tool, agent, status}            — counter
tool_dispatch_duration_seconds{tool, agent}          — histogram (0.1, 0.25, 0.5, 1, 2.5, 5, 10, 30)
llm_requests_total{model, provider, status}          — counter
llm_request_duration_seconds{model, provider}        — histogram (0.1..60)

<!-- Loki log format (JSON structured, from slog) -->
Fields in every log line: time, level, msg, correlation_id, source (file:line)
Additional fields vary by service (business_id, tool_name, etc.)

<!-- Grafana datasource names (from plan-01 provisioning) -->
Loki datasource name: "Loki"
Prometheus datasource name: "Prometheus"

<!-- Scrape job names (from plan-01 prometheus.yml) -->
job="api" — scrapes api:8080/metrics
job="orchestrator" — scrapes orchestrator:8090/metrics
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Request Trace dashboard (Loki — correlation_id search)</name>
  <files>observability/grafana/dashboards/request-trace.json</files>
  <read_first>
    observability/grafana/provisioning/datasources/datasources.yml,
    observability/grafana/provisioning/dashboards/dashboards.yml
  </read_first>
  <action>
Create a Grafana dashboard JSON file at observability/grafana/dashboards/request-trace.json.

Dashboard properties:
- title: "Request Trace"
- uid: "request-trace"
- tags: ["onevoice", "logs", "trace"]
- timezone: "browser"
- editable: true
- refresh: "10s"
- time range: last 1 hour

Template variables:
- Variable name: "correlation_id", label: "Correlation ID", type: "textbox", default: ""

Panels (use Loki datasource by name "Loki"):

1. **Panel: "Logs by Correlation ID"** — type: logs
   - gridPos: h=20, w=24, x=0, y=0
   - Loki query: `{job="docker"} |= "$correlation_id"` (when correlation_id is not empty)
   - Alternative if correlation_id is empty: `{job="docker"} | json | __error__=""` (shows all structured logs)
   - Use the templating variable: `${correlation_id}`
   - LogQL: `{job="docker"} |= "${correlation_id}" | json`
   - Sort: time ascending (oldest first) so the trace reads chronologically
   - Show labels: container
   - Enable wrapping

2. **Panel: "Service Timeline"** — type: timeseries
   - gridPos: h=8, w=24, x=0, y=20
   - Loki query: `sum by (container) (count_over_time({job="docker"} |= "${correlation_id}" | json [1m]))`
   - Legend: {{container}}
   - This shows which services processed this correlation_id and when

The datasource in each panel target should use: `{"type": "loki", "uid": "loki"}` — but note the uid depends on how Grafana auto-assigns it. Use the datasource by name approach: set `"datasource": {"type": "loki", "uid": "P1"}` where P1 is a placeholder. Actually, for provisioned datasources, use the name directly:
```json
"datasource": {
  "type": "loki",
  "uid": "${DS_LOKI}"
}
```
Or more reliably, just set datasource to the string "Loki" which Grafana resolves by name.

Use the simplest approach: `"datasource": "Loki"` as string. Grafana resolves provisioned datasource names.

The JSON must be a complete, valid Grafana dashboard JSON (with __inputs, __requires, or just directly referencing datasource names).
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('observability/grafana/dashboards/request-trace.json')); assert d['title']=='Request Trace'; assert any('correlation_id' in str(p) for p in d['panels']); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File is valid JSON
    - Dashboard title is "Request Trace"
    - Has a templating variable named "correlation_id"
    - At least one panel uses LogQL query containing "correlation_id"
    - Panels reference Loki datasource
    - Dashboard uid is "request-trace"
  </acceptance_criteria>
  <done>Request Trace dashboard JSON provisioned — entering a correlation_id filters logs from all services in chronological order.</done>
</task>

<task type="auto">
  <name>Task 2: Create Metrics Overview dashboard (Prometheus — latency, errors, tools)</name>
  <files>observability/grafana/dashboards/metrics-overview.json</files>
  <read_first>
    observability/grafana/provisioning/datasources/datasources.yml,
    pkg/metrics/middleware.go,
    pkg/metrics/tools.go,
    pkg/metrics/llm.go
  </read_first>
  <action>
Create a Grafana dashboard JSON file at observability/grafana/dashboards/metrics-overview.json.

Dashboard properties:
- title: "Metrics Overview"
- uid: "metrics-overview"
- tags: ["onevoice", "metrics", "prometheus"]
- timezone: "browser"
- editable: true
- refresh: "30s"
- time range: last 1 hour

Template variables:
- Variable name: "job", label: "Service", type: "query", query: "label_values(http_requests_total, job)", multi: true, includeAll: true, allValue: ".*"

Use "Prometheus" datasource (by name string).

Row 1: HTTP Performance (y=0)

1. **Panel: "HTTP Request Latency (p50/p95/p99)"** — type: timeseries
   - gridPos: h=8, w=12, x=0, y=0
   - Queries:
     - A: `histogram_quantile(0.50, sum(rate(http_request_duration_seconds_bucket{job=~"$job"}[5m])) by (le, job))` legend: "{{job}} p50"
     - B: `histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{job=~"$job"}[5m])) by (le, job))` legend: "{{job}} p95"
     - C: `histogram_quantile(0.99, sum(rate(http_request_duration_seconds_bucket{job=~"$job"}[5m])) by (le, job))` legend: "{{job}} p99"
   - Y-axis unit: seconds (s)
   - Title: "HTTP Request Latency"

2. **Panel: "Error Rate by Service"** — type: timeseries
   - gridPos: h=8, w=12, x=12, y=0
   - Query: `sum(rate(http_requests_total{job=~"$job", status=~"5.."}[5m])) by (job) / sum(rate(http_requests_total{job=~"$job"}[5m])) by (job) * 100`
   - Legend: "{{job}}"
   - Y-axis unit: percent (0-100)
   - Title: "Error Rate %"
   - Thresholds: 1% warning (yellow), 5% critical (red)

Row 2: Request Volume (y=8)

3. **Panel: "Requests per Second"** — type: timeseries
   - gridPos: h=8, w=12, x=0, y=8
   - Query: `sum(rate(http_requests_total{job=~"$job"}[5m])) by (job)`
   - Legend: "{{job}}"
   - Y-axis unit: reqps
   - Title: "Requests / sec"

4. **Panel: "Status Code Distribution"** — type: piechart
   - gridPos: h=8, w=12, x=12, y=8
   - Query: `sum(increase(http_requests_total{job=~"$job"}[$__range])) by (status)`
   - Legend: "{{status}}"
   - Title: "Status Code Distribution"

Row 3: Tool Dispatch (y=16)

5. **Panel: "Tool Dispatch Duration"** — type: timeseries (histogram)
   - gridPos: h=8, w=12, x=0, y=16
   - Queries:
     - A: `histogram_quantile(0.50, sum(rate(tool_dispatch_duration_seconds_bucket[5m])) by (le))` legend: "p50"
     - B: `histogram_quantile(0.95, sum(rate(tool_dispatch_duration_seconds_bucket[5m])) by (le))` legend: "p95"
     - C: `histogram_quantile(0.99, sum(rate(tool_dispatch_duration_seconds_bucket[5m])) by (le))` legend: "p99"
   - Y-axis unit: seconds
   - Title: "Tool Dispatch Latency"

6. **Panel: "Tool Dispatch Rate by Tool"** — type: timeseries
   - gridPos: h=8, w=12, x=12, y=16
   - Query: `sum(rate(tool_dispatch_total[5m])) by (tool, status)`
   - Legend: "{{tool}} ({{status}})"
   - Title: "Tool Dispatches / sec"

Row 4: LLM (y=24, bonus — metrics exist)

7. **Panel: "LLM Request Latency"** — type: stat
   - gridPos: h=4, w=8, x=0, y=24
   - Query: `histogram_quantile(0.95, sum(rate(llm_request_duration_seconds_bucket[5m])) by (le))`
   - Unit: seconds
   - Title: "LLM p95 Latency"

8. **Panel: "LLM Requests by Provider"** — type: timeseries
   - gridPos: h=8, w=16, x=8, y=24
   - Query: `sum(rate(llm_requests_total[5m])) by (provider, status)`
   - Legend: "{{provider}} ({{status}})"
   - Title: "LLM Requests / sec"

The JSON must be a complete, valid Grafana dashboard model with all panels, targets, and proper gridPos layout.
  </action>
  <verify>
    <automated>python3 -c "import json; d=json.load(open('observability/grafana/dashboards/metrics-overview.json')); assert d['title']=='Metrics Overview'; panels=[p['title'] for p in d['panels'] if 'title' in p]; assert 'HTTP Request Latency' in panels; assert 'Tool Dispatch Latency' in panels; print('OK: %d panels' % len(d['panels']))"</automated>
  </verify>
  <acceptance_criteria>
    - File is valid JSON
    - Dashboard title is "Metrics Overview"
    - Dashboard uid is "metrics-overview"
    - Has panels for: HTTP Request Latency (with p50/p95/p99 queries), Error Rate by Service, Tool Dispatch Latency
    - Panels reference Prometheus datasource
    - PromQL queries use correct metric names: http_request_duration_seconds_bucket, http_requests_total, tool_dispatch_duration_seconds_bucket, tool_dispatch_total
    - Has template variable "job" for service filtering
  </acceptance_criteria>
  <done>Metrics Overview dashboard provisioned with HTTP latency percentiles, error rate, and tool dispatch histogram panels.</done>
</task>

</tasks>

<verification>
Run: `python3 -c "import json, glob; files=glob.glob('observability/grafana/dashboards/*.json'); [json.load(open(f)) for f in files]; print(f'{len(files)} valid dashboard JSON files')"`
Expected: "2 valid dashboard JSON files"

Run: `grep -l "correlation_id" observability/grafana/dashboards/*.json`
Expected: request-trace.json

Run: `grep -l "http_request_duration_seconds" observability/grafana/dashboards/*.json`
Expected: metrics-overview.json

Run: `grep -l "tool_dispatch_duration_seconds" observability/grafana/dashboards/*.json`
Expected: metrics-overview.json
</verification>

<success_criteria>
- Two dashboard JSON files exist and are valid JSON
- Request Trace dashboard has correlation_id template variable and LogQL queries
- Metrics Overview dashboard has HTTP latency p50/p95/p99, error rate, and tool dispatch panels
- All dashboards use correct datasource references (Loki, Prometheus)
- Dashboards will auto-load on Grafana startup via provisioning (from plan-01 config)
</success_criteria>

<output>
After completion, create `.planning/phases/08-grafana-+-loki-stack/08-02-SUMMARY.md`
</output>
